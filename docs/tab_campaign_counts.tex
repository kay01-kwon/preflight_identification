% Trial counts of every campaign, one row per condition structure.
% Counts verified against the shipped datasets: SimDataSet/R3 (308),
% DataSet/exp (140), DataSet/loadcell (5 x 2 x 5), SimDataSet/
% free_flight (132), DataSet/free_flight (90).
\begin{table}[t]
\caption{Trial counts per campaign and condition. Every reported
statistic draws on the full set of its row; the excitation gates are
the only exclusions and are listed exhaustively in
Table~\ref{tab:sim_gate_exclusions}.}
\label{tab:campaign_counts}
\centering\small
\begin{tabular}{@{}llc@{}}
\toprule
Campaign & Conditions & Trials\\
\midrule
Simulation excitation & 11 cases $\times$ 2 axes $\times$
 2 directions $\times$ 7 ramp rates & 308\\
 & \quad passing the run gates & 278\\
Hardware excitation & 5 cases $\times$ 2 axes $\times$
 2 directions $\times$ 7 ramp rates & 140\\
 & \quad passing the run gates & 140\\
Load-cell reference & 5 cases $\times$ 2 axes $\times$ 5 repeats & 50\\
Simulation free flight & 11 cases $\times$ 2 controllers $\times$
 2 variants $\times$ 3 flights & 132\\
Hardware free flight & 5 cases $\times$ 2 controllers $\times$
 3 variants $\times$ 3 flights & 90\\
Shifted-moment validation & 7 ramp rates $\times$ 30 noise
 realisations & 210\\
\bottomrule
\end{tabular}
\end{table}
